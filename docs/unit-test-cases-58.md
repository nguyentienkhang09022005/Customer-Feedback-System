| Test Case ID | Req. ID | UC Id | Unit Id | Unit Section Id | Pre-Condition | Test Case Description | Input Type | Input | Expected Result | Actual Output | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|
| U_AUTH_01 | REQ-01 | UC-01 | register_customer | AuthService | `db_session`, `mock_redis_service`, `mock_email_service`; chưa có customer trùng email/username/phone; OTPService được mock trả về True. | Kiểm tra đăng ký khách hàng thành công khi dữ liệu hợp lệ và hệ thống có thể tạo/lưu OTP. | Valid | `RegisterRequest(username='newcustomer', password='password123', email='newcustomer@example.com', first_name='New', last_name='Customer', phone='1234567890', address='123 New St', timezone='Asia/Ho_Chi_Minh')` | Hàm trả về `True`; bản ghi Customer được tạo trong DB với `status=PENDING`; dữ liệu cơ bản được lưu đúng. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_02 | REQ-02 | UC-01 | register_customer | AuthService | `db_session`, `sample_customer`, `mock_redis_service`; đã tồn tại customer có cùng email. | Kiểm tra đăng ký khách hàng bị từ chối khi email đã tồn tại. | Invalid | `RegisterRequest(... email=sample_customer.email ...)` | Hàm trả về `False`; không tạo customer mới trong DB. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_03 | REQ-03 | UC-02 | verify_otp_and_activate | AuthService | `db_session`, `sample_customer_pending`, `mock_redis_service`; `OTPService.verify_otp_code` được mock trả về `True`; customer đang ở trạng thái `PENDING`. | Kiểm tra xác thực OTP thành công sẽ kích hoạt tài khoản khách hàng. | Valid | `email=sample_customer_pending.email`, `otp_code='123456'` | Customer được cập nhật `status=ACTIVE` trong DB; hàm trả về customer đã kích hoạt. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_04 | REQ-04 | UC-02 | verify_otp_and_activate | AuthService | `db_session`, `sample_customer_pending`, `mock_redis_service`; mock OTP đúng nhưng email không khớp customer pending nào. | Kiểm tra xác thực OTP thất bại khi email không thuộc user đang chờ kích hoạt. | Invalid | `email='wrong@example.com'`, `otp_code='123456'` | Hàm trả về `None`; không có customer nào được kích hoạt. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_05 | REQ-05 | UC-03 | authenticate_user | AuthService | `db_session`, `sample_customer`; customer ở trạng thái Active và password hash hợp lệ. | Kiểm tra xác thực người dùng thành công với username/password đúng. | Valid | `username='customer1'`, `password='321321'` | Hàm trả về đối tượng user; kiểm tra bcrypt hợp lệ và status Active. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_06 | REQ-06 | UC-03 | authenticate_user | AuthService | `db_session`; tạo customer inline với trạng thái `PENDING`. | Kiểm tra xác thực thất bại nếu tài khoản chưa Active. | Invalid | `username='pendinguser'`, `password='password123'` | Hàm trả về `None`; không cho đăng nhập với tài khoản `PENDING`. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_07 | REQ-07 | UC-04 | create_tokens | AuthService | `db_session`, `sample_customer`; user hợp lệ đã tồn tại. | Kiểm tra tạo cặp access token/refresh token thành công cho user hợp lệ. | Valid | `user=sample_customer` | Hàm trả về tuple `(access_token, refresh_token)` đều khác rỗng; payload JWT chứa `jti`. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_08 | REQ-08 | UC-04 | create_tokens / verify_access_token | AuthService | `db_session`, `sample_employee` có `role_name='Admin'`. | Kiểm tra access token chứa đúng claim cho employee/admin. | Valid | `user=sample_employee` → verify `access_token` | Payload chứa `user_type='employee'`, `role='Admin'`, `email` đúng, `sub` là UUID string. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_09 | REQ-09 | UC-05 | refresh_tokens | AuthService | `db_session`, `sample_customer`; tạo trước refresh token hợp lệ từ `create_tokens`. | Kiểm tra refresh token hợp lệ sinh ra cặp token mới. | Valid | `refresh_token` hợp lệ được tạo từ `service.create_tokens(sample_customer)` | Hàm trả về cặp token mới khác rỗng; user được lấy lại từ DB và token mới được tạo. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_10 | REQ-10 | UC-05 | refresh_tokens | AuthService | `db_session`; không cần user hợp lệ vì truyền token sai định dạng. | Kiểm tra refresh thất bại khi token không hợp lệ. | Invalid | `refresh_token='invalid.refresh.token'` | Hàm trả về `None`; không có DB write, không sinh token mới. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_11 | REQ-11 | UC-05 | verify_token | security.py | Có token kiểu `refresh` đã được tạo sẵn. | Kiểm tra hàm verify token trả về `None` khi loại token không khớp kỳ vọng. | Invalid | `verify_token(refresh_token, 'access')` | Hàm trả về `None`; chặn việc dùng refresh token như access token. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_12 | REQ-12 | UC-06 | change_password | AuthService | `db_session`, `sample_customer`; user tồn tại, mật khẩu cũ hợp lệ. | Kiểm tra đổi mật khẩu thành công khi mật khẩu cũ đúng. | Valid | `user_id=sample_customer.id_customer`, `old_password='321321'`, `new_password='newpassword123'` | Hàm trả về `True`; password hash trong DB được cập nhật sang mật khẩu mới. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_13 | REQ-13 | UC-06 | change_password | AuthService | `db_session`, `sample_customer`; user tồn tại nhưng nhập sai mật khẩu cũ. | Kiểm tra đổi mật khẩu thất bại khi mật khẩu cũ không đúng. | Invalid | `old_password='wrongoldpassword'`, `new_password='newpassword123'` | Hàm trả về `False`; không cập nhật mật khẩu trong DB. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_14 | REQ-14 | UC-06 | change_password | AuthService | `db_session`; không có user tương ứng với UUID truyền vào. | Kiểm tra đổi mật khẩu thất bại khi user không tồn tại. | Invalid | `user_id=str(uuid4())`, `old_password='oldpassword'`, `new_password='newpassword'` | Hàm trả về `False`; không có password write. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_15 | REQ-15 | UC-07 | initiate_forgot_password | AuthService | `db_session`, `mock_redis_service`; email không tồn tại trong hệ thống. | Kiểm tra quên mật khẩu không làm lộ việc email có tồn tại hay không. | Invalid | `email='nonexistent@example.com'` | Hàm vẫn trả về `True` để chống email enumeration; không phát sinh lỗi cho người dùng cuối. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_16 | REQ-16 | UC-07 | reset_password_with_otp | AuthService | `db_session`, `sample_customer`, `mock_redis_service`; `OTPService.verify_password_reset_otp` mock trả về `True`. | Kiểm tra đặt lại mật khẩu bằng OTP thành công. | Valid | `email=sample_customer.email`, `otp_code='123456'`, `new_password='newresetpassword'` | Hàm trả về `True`; password hash được cập nhật trong DB. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_17 | REQ-17 | UC-07 | reset_password_with_otp | AuthService | `db_session`, `sample_customer`, `mock_redis_service`; OTPService mock trả về `False`. | Kiểm tra đặt lại mật khẩu thất bại khi OTP sai. | Invalid | `email=sample_customer.email`, `otp_code='999999'`, `new_password='newpassword'` | Hàm trả về `False`; mật khẩu trong DB không bị thay đổi. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_18 | REQ-18 | UC-08 | update_profile | AuthService | `db_session`, `sample_customer`; user tồn tại. | Kiểm tra cập nhật đầy đủ thông tin hồ sơ người dùng thành công. | Valid | `user_id=sample_customer.id_customer`, `UserUpdateRequest(first_name='UpdatedFirst', last_name='UpdatedLast', phone='9998887777', address='456 Updated Ave')` | Hàm trả về user đã cập nhật; các field được ghi đúng và commit vào DB. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_19 | REQ-19 | UC-08 | update_profile | AuthService | `db_session`, `sample_customer`; user tồn tại. | Kiểm tra cập nhật một phần hồ sơ không làm mất dữ liệu ở các field không truyền vào. | Valid | `UserUpdateRequest(first_name='OnlyFirstUpdated')` | Chỉ `first_name` thay đổi; các field khác như `last_name` giữ nguyên. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_20 | REQ-20 | UC-08 | update_profile | AuthService | `db_session`; không có user tương ứng. | Kiểm tra cập nhật hồ sơ trả về `None` khi user không tồn tại. | Invalid | `user_id=uuid4()`, `UserUpdateRequest(first_name='X')` | Hàm trả về `None`; không commit thay đổi nào vào DB. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_21 | REQ-21 | UC-09 | verify_otp_code | OTPService | Fake Redis store đã pre-populate key `otp:test@example.com` với OTP đúng và `type='verification'`. | Kiểm tra xác thực OTP đăng ký thành công và OTP bị xóa sau khi dùng. | Valid | `email='test@example.com'`, `otp='123456'` | Hàm trả về `True`; key OTP bị xóa khỏi Redis để không dùng lại được. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_22 | REQ-22 | UC-09 | verify_otp_code | OTPService | Fake Redis store có OTP hợp lệ, cùng OTP được gọi 2 lần liên tiếp. | Kiểm tra OTP chỉ dùng được một lần. | Invalid | Lần 1: `verify_otp_code(email,'123456')`; Lần 2: gọi lại cùng OTP | Lần 1 trả về `True`; lần 2 trả về `False` vì OTP đã bị xóa sau lần đầu. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_23 | REQ-23 | UC-09 | verify_password_reset_otp | OTPService | Redis có entry OTP nhưng `type='verification'` thay vì `password_reset`. | Kiểm tra OTP đăng ký không được dùng cho luồng reset password. | Invalid | `email`, `otp='123456'`, cache entry có `type='verification'` | Hàm trả về `False`; từ chối OTP sai ngữ cảnh sử dụng. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_24 | REQ-24 | UC-10 | blacklist_access_token | TokenBlacklistService | Patch `redis_service.set_with_expiry.return_value=False`; Redis giả lập lỗi. | Kiểm tra blacklist access token trả về False khi Redis ghi thất bại. | Invalid | `jti=str(uuid4())`, `user_id=str(uuid4())`, `expires_in=3600` | Hàm trả về `False`; token không được blacklist thành công. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_25 | REQ-25 | UC-11 | create_customer | CustomerService | `db_session`, `sample_customer_type`; không trùng dữ liệu với customer khác. | Kiểm tra tạo customer thành công bằng CustomerService. | Valid | `CustomerCreate(...)` hợp lệ với email/username/phone mới | Customer được tạo thành công trong DB. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_26 | REQ-26 | UC-11 | create_customer | CustomerService | `db_session`, `sample_customer_type`, `sample_customer`; email đã tồn tại. | Kiểm tra tạo customer thất bại khi email bị trùng. | Invalid | `CustomerCreate(... email=sample_customer.email ...)` | Ném ra Exception/HTTPException 400 với nội dung báo đã tồn tại. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_27 | REQ-27 | UC-11 | update_customer | CustomerService | `db_session`; truyền `id_customer` không tồn tại. | Kiểm tra cập nhật customer thất bại khi không tìm thấy bản ghi. | Invalid | `customer_id=str(uuid4())`, `CustomerUpdate(...)` | Ném ra Exception/HTTPException 404. | Đúng với kết quả mong đợi | Pass |
| U_AUTH_28 | REQ-28 | UC-11 | delete_customer | CustomerService | `db_session`, `sample_customer`; customer đang tồn tại. | Kiểm tra xóa customer thành công. | Valid | `customer_id=sample_customer.id_customer` | Customer bị xóa khỏi DB; truy vấn lại không còn bản ghi. | Đúng với kết quả mong đợi | Pass |
| U_EMP_01 | REQ-29 | UC-12 | create_employee | EmployeeService | `employee_service`, `sample_department`, `sample_role_employee`; email/username/phone chưa tồn tại. | Kiểm tra tạo employee thành công và sinh mã nhân viên tự động. | Valid | `EmployeeCreate(username='newemployee', email='newemployee@example.com', password='321321', id_department=sample_department.id_department, role_name=sample_role_employee.role_name)` | Employee được tạo; `employee_code` bắt đầu bằng `NV`; status Active; các giá trị mặc định được set. | Đúng với kết quả mong đợi | Pass |
| U_EMP_02 | REQ-30 | UC-12 | create_employee | EmployeeService | Có `sample_employee` trong DB với email trùng. | Kiểm tra tạo employee thất bại khi email bị trùng. | Invalid | `EmployeeCreate(... email=sample_employee.email ...)` | Ném `HTTPException` 400 với thông điệp chứa `tồn tại`. | Đúng với kết quả mong đợi | Pass |
| U_EMP_03 | REQ-31 | UC-12 | create_employee | EmployeeService | Có `sample_employee` trong DB với username trùng. | Kiểm tra tạo employee thất bại khi username bị trùng. | Invalid | `EmployeeCreate(... username=sample_employee.username ...)` | Ném `HTTPException` 400 với thông điệp chứa `tồn tại`. | Đúng với kết quả mong đợi | Pass |
| U_EMP_04 | REQ-32 | UC-12 | create_employee | EmployeeService | Có `sample_employee` trong DB với phone trùng. | Kiểm tra tạo employee thất bại khi số điện thoại bị trùng. | Invalid | `EmployeeCreate(... phone=sample_employee.phone ...)` | Ném `HTTPException` 400 với thông điệp chứa `tồn tại`. | Đúng với kết quả mong đợi | Pass |
| U_EMP_05 | REQ-33 | UC-13 | update_employee | EmployeeService | `employee_service`; tạo `admin_employee` và `non_admin`; `non_admin` không phải Admin. | Kiểm tra non-admin không được chỉnh sửa tài khoản Admin khác. | Invalid | `EmployeeUpdate(first_name='Hacked')`, `current_user=non_admin`, target=`admin_employee.id_employee` | Ném `HTTPException` 403 với thông điệp không có quyền cập nhật Admin khác. | Đúng với kết quả mong đợi | Pass |
| U_EMP_06 | REQ-34 | UC-13 | manager_update_employee | EmployeeService | `employee_service`, `sample_manager`, `outsider` thuộc department khác. | Kiểm tra manager không được cập nhật nhân viên ngoài phòng ban của mình. | Invalid | `EmployeeUpdate(job_title='Hacked Title')`, `current_manager=sample_manager`, target outsider | Ném `HTTPException` 403 với thông điệp liên quan phòng ban. | Đúng với kết quả mong đợi | Pass |
| U_EMP_07 | REQ-35 | UC-14 | delete_employee | EmployeeService | `employee_service`, tạo `admin_emp` và `sample_employee_2` role thấp hơn. | Kiểm tra non-admin không được xóa tài khoản Admin. | Invalid | `delete_employee(admin_emp.id_employee, current_user=sample_employee_2)` | Ném `HTTPException` 403. | Đúng với kết quả mong đợi | Pass |
| U_EMP_08 | REQ-36 | UC-14 | delete_employee | EmployeeService | `employee_service`; tạo employee hợp lệ và commit vào DB. | Kiểm tra xóa employee thành công theo nhánh happy path. | Valid | `delete_employee(emp.id_employee)` | Hàm chạy thành công; truy vấn lại employee theo id sẽ báo 404/không tìm thấy. | Đúng với kết quả mong đợi | Pass |
| U_DEPT_01 | REQ-37 | UC-15 | get_all_department_assignments | DepartmentAssignmentService | `db_session`, `sample_employee`, `sample_employee_2` có sẵn. | Kiểm tra lấy danh sách phân công phòng ban trả về list đầy đủ. | Valid | Không có tham số đầu vào | Hàm trả về `list` chứa các phần tử `EmployeeAssignmentOut` của các employee mẫu. | Đúng với kết quả mong đợi | Pass |
| U_DEPT_02 | REQ-38 | UC-15 | get_department_with_members | DepartmentAssignmentService | `db_session`, `sample_department`, `sample_manager`; department có manager. | Kiểm tra lấy thông tin department kèm manager khi manager tồn tại. | Valid | `department_id=sample_department.id_department` | `result.manager` khác `None`, đúng `employee_id` của manager, `is_manager=True`; manager không nằm trong `members`. | Đúng với kết quả mong đợi | Pass |
| U_DEPT_03 | REQ-39 | UC-15 | get_department_with_members | DepartmentAssignmentService | `db_session`, `sample_department_2`; department không có manager. | Kiểm tra lấy department khi không có manager vẫn xử lý null-safe. | Valid | `department_id=sample_department_2.id_department` | `result.manager is None`; không phát sinh exception. | Đúng với kết quả mong đợi | Pass |
| U_SLA_01 | REQ-40 | UC-16 | create_policy | SLAService | `db_session`; DB rỗng hoặc chưa có policy tương ứng. | Kiểm tra tạo SLA policy thành công với dữ liệu chuẩn. | Valid | `SLACreate(policy_name='Critical Response', severity='Critical', max_resolution_days=1)` | Policy được tạo; `is_active=True`, severity và số ngày được lưu đúng. | Đúng với kết quả mong đợi | Pass |
| U_SLA_02 | REQ-41 | UC-16 | create_policy | SLAService | `db_session`; không có ràng buộc ngăn `max_resolution_days=0`. | Kiểm tra tạo SLA policy thành công với biên bằng 0 ngày. | Valid | `SLACreate(... max_resolution_days=0)` | Policy được tạo với `max_resolution_days == 0`. | Đúng với kết quả mong đợi | Pass |
| U_SLA_03 | REQ-42 | UC-16 | update_policy | SLAService | `db_session`; không có policy nào mang UUID truyền vào. | Kiểm tra update SLA policy thất bại khi policy không tồn tại. | Invalid | `policy_id=uuid4()`, `SLAUpdate(policy_name='Test')` | Ném `HTTPException` 404 với nội dung `không tồn tại`. | Đúng với kết quả mong đợi | Pass |
| U_SLA_04 | REQ-43 | UC-16 | get_all_policies | SLAService | `db_session`, `sample_sla_policy` tồn tại. | Kiểm tra lấy toàn bộ SLA policies trả về ít nhất một phần tử. | Valid | Không có tham số | Hàm trả về danh sách policies, bao gồm policy mẫu. | Đúng với kết quả mong đợi | Pass |
| U_SLA_05 | REQ-44 | UC-16 | toggle_policy | SLAService | `db_session`, `sample_sla_policy.is_active=True`. | Kiểm tra toggle policy từ active sang inactive. | Valid | `policy_id=sample_sla_policy.id_policy` | `policy.is_active` chuyển thành `False`. | Đúng với kết quả mong đợi | Pass |
| U_SLA_06 | REQ-45 | UC-16 | toggle_policy | SLAService | `db_session`, `sample_sla_policy` đã được set `is_active=False`. | Kiểm tra toggle policy từ inactive sang active. | Valid | `policy_id=sample_sla_policy.id_policy` | `policy.is_active` chuyển thành `True`. | Đúng với kết quả mong đợi | Pass |
| U_SLA_07 | REQ-46 | UC-16 | toggle_policy | SLAService | `db_session`; không tồn tại policy tương ứng. | Kiểm tra toggle policy thất bại khi không tìm thấy policy. | Invalid | `policy_id=uuid4()` | Ném `HTTPException` 404 với nội dung `không tồn tại`. | Đúng với kết quả mong đợi | Pass |
| U_TICK_01 | REQ-47 | UC-17 | create_ticket_from_template | TicketService | `db_session`, `sample_customer`, `sample_ticket_template`, `mock_redis_service`; template hợp lệ và customer hợp lệ. | Kiểm tra tạo ticket từ template thành công. | Valid | `TicketFromTemplateCreate(title='...', severity='High', id_template=sample_ticket_template.id_template, custom_fields={...})` | Ticket được tạo; `status='New'`, severity được set, `id_customer` khớp. | Đúng với kết quả mong đợi | Pass |
| U_TICK_02 | REQ-48 | UC-17 | create_ticket_from_template | TicketService | Template có `severity.default_value='Medium'`; request không truyền severity. | Kiểm tra ticket kế thừa severity mặc định từ template. | Valid | `TicketFromTemplateCreate(title='...', id_template=sample_ticket_template.id_template, custom_fields={...})` | `ticket.severity == 'Medium'`. | Đúng với kết quả mong đợi | Pass |
| U_TICK_03 | REQ-49 | UC-17 | create_ticket_from_template | TicketService | `db_session`; template UUID truyền vào không tồn tại. | Kiểm tra tạo ticket thất bại khi không tìm thấy template. | Invalid | `id_template=uuid4()` | Ném `HTTPException` 404 `Không tìm thấy template`. | Đúng với kết quả mong đợi | Pass |
| U_TICK_04 | REQ-50 | UC-17 | create_ticket_from_template | TicketService | Template đã bị soft-delete (`is_deleted=True`). | Kiểm tra tạo ticket thất bại khi template đã bị xóa mềm. | Invalid | `id_template` của template deleted | Ném `HTTPException` 400 `Template đã bị xóa`. | Đúng với kết quả mong đợi | Pass |
| U_TICK_05 | REQ-51 | UC-17 | create_ticket_from_template | TicketService | Template tồn tại nhưng `is_active=False`. | Kiểm tra tạo ticket thất bại khi template không còn hoạt động. | Invalid | `id_template` của template inactive | Ném `HTTPException` 400 `Template không còn hoạt động`. | Đúng với kết quả mong đợi | Pass |
| U_TICK_06 | REQ-52 | UC-17 | create_ticket_from_template | TicketService | Mock Redis rate-limit trả count vượt ngưỡng `RATE_LIMIT_TICKETS=5`. | Kiểm tra tạo ticket bị chặn bởi rate limit. | Invalid | Request tạo ticket hợp lệ nhưng Redis trả count=10 | Ném `HTTPException` 429 `Bạn đã tạo quá nhiều tickets`. | Đúng với kết quả mong đợi | Pass |
| U_TICK_07 | REQ-53 | UC-18 | update_ticket | TicketService | `db_session`, `sample_ticket.status='New'`. | Kiểm tra employee cập nhật trạng thái ticket thành công theo transition hợp lệ. | Valid | `TicketUpdate(status='In Progress')`, `actor_type='employee'` | `ticket.status == 'In Progress'`. | Đúng với kết quả mong đợi | Pass |
| U_TICK_08 | REQ-54 | UC-18 | update_ticket | TicketService | `db_session`, `sample_ticket.status='New'`. | Kiểm tra update thất bại khi chuyển trạng thái trực tiếp không hợp lệ. | Invalid | `TicketUpdate(status='Resolved')`, `actor_type='employee'` | Ném `HTTPException` 400 vì transition `New -> Resolved` không hợp lệ. | Đúng với kết quả mong đợi | Pass |
| U_TICK_09 | REQ-55 | UC-18 | update_ticket | TicketService | `db_session`, `sample_ticket` còn active; actor là employee. | Kiểm tra employee không được cập nhật title/custom_fields của ticket. | Invalid | `TicketUpdate(title='Changed Title', custom_fields={'new':'field'})`, `actor_type='employee'` | Ném `HTTPException` 403 `Employee chỉ được cập nhật trạng thái ticket`. | Đúng với kết quả mong đợi | Pass |
| U_TICK_10 | REQ-56 | UC-18 | update_ticket | TicketService | `sample_ticket.is_deleted=True`. | Kiểm tra update thất bại khi ticket đã bị xóa mềm. | Invalid | `TicketUpdate(status='In Progress')`, `actor_type='employee'` | Ném `HTTPException` 400 `Ticket đã bị xóa`. | Đúng với kết quả mong đợi | Pass |
| U_TICK_11 | REQ-57 | UC-18 | update_ticket | TicketService | `db_session`, `sample_ticket`, `sample_sla_policy(High=1 day)`, `mock_redis_service`. | Kiểm tra đổi severity sẽ tính lại expired_date theo SLA. | Valid | `TicketUpdate(severity='High')`, `actor_type='employee'` | Severity đổi thành `High`; `expired_date` được tính lại khác `None`. | Đúng với kết quả mong đợi | Pass |
| U_TICK_12 | REQ-58 | UC-19 | update_ticket_customer | TicketService | `db_session`, ticket thuộc customer và đang `status='New'`. | Kiểm tra customer cập nhật ticket của chính mình thành công khi ticket còn New. | Valid | `TicketCustomerUpdate(title='updated title', custom_fields={...})` | Ticket được cập nhật thành công; title/custom_fields thay đổi. | Đúng với kết quả mong đợi | Pass |
| U_TICK_13 | REQ-59 | UC-19 | update_ticket_customer | TicketService | Ticket của customer nhưng đang ở trạng thái `In Progress`. | Kiểm tra customer không được cập nhật ticket khi ticket không còn New. | Invalid | `TicketCustomerUpdate(...)` trên ticket `status='In Progress'` | Ném `HTTPException` 400 `Chỉ được cập nhật khi ticket còn ở trạng thái New`. | Đúng với kết quả mong đợi | Pass |
| U_TICK_14 | REQ-60 | UC-19 | update_ticket_customer | TicketService | Ticket thuộc Customer A; request được gọi với Customer B. | Kiểm tra customer không được cập nhật ticket của người khác. | Invalid | `customer_id` không khớp `ticket.id_customer` | Ném `HTTPException` 403 `Bạn không có quyền cập nhật`. | Đúng với kết quả mong đợi | Pass |
| U_TICK_15 | REQ-61 | UC-20 | assign_ticket | TicketService | Ticket hợp lệ chưa xóa; employee tồn tại. | Kiểm tra gán ticket cho employee thành công. | Valid | `ticket_id`, `employee_id` hợp lệ | `ticket.id_employee` được set bằng `employee.id_employee`. | Đúng với kết quả mong đợi | Pass |
| U_TICK_16 | REQ-62 | UC-20 | assign_ticket | TicketService | Ticket không tồn tại trong DB. | Kiểm tra gán ticket thất bại khi ticket không tồn tại. | Invalid | `ticket_id=uuid4()`, `employee_id` hợp lệ | Ném `HTTPException` 404 `Không tìm thấy ticket`. | Đúng với kết quả mong đợi | Pass |
| U_TICK_17 | REQ-63 | UC-20 | assign_ticket | TicketService | Ticket tồn tại nhưng `is_deleted=True`. | Kiểm tra gán ticket thất bại khi ticket đã bị xóa mềm. | Invalid | `ticket_id` của deleted ticket, `employee_id` hợp lệ | Ném `HTTPException` 400 `Ticket đã bị xóa`. | Đúng với kết quả mong đợi | Pass |
| U_TICK_18 | REQ-64 | UC-21 | resolve_ticket | TicketService | Ticket đang `status='In Progress'`; có `resolution_note`. | Kiểm tra resolve ticket thành công từ trạng thái hợp lệ. | Valid | `ticket_id`, `resolution_note='Fixed'` | Ticket chuyển sang `Resolved`; `resolved_at` được set; `resolution_note` được lưu. | Đúng với kết quả mong đợi | Pass |
| U_TICK_19 | REQ-65 | UC-21 | resolve_ticket | TicketService | Ticket đang `status='New'`. | Kiểm tra resolve ticket thất bại khi chuyển từ trạng thái `New`. | Invalid | `ticket_id` của ticket `status='New'` | Ném `HTTPException` 400 `Không thể giải quyết ticket`. | Đúng với kết quả mong đợi | Pass |
| U_TICK_20 | REQ-66 | UC-22 | close_ticket | TicketService | Ticket đang `status='Resolved'`, có lý do đóng. | Kiểm tra đóng ticket thành công từ `Resolved`. | Valid | `ticket_id`, `reason='Customer satisfied'` | Ticket chuyển sang `Closed`; history closure được log. | Đúng với kết quả mong đợi | Pass |
| U_TICK_21 | REQ-67 | UC-22 | close_ticket | TicketService | Ticket đang `status='In Progress'`. | Kiểm tra đóng ticket thất bại khi ticket chưa ở trạng thái `Resolved`. | Invalid | `ticket_id` của ticket `status='In Progress'`, `reason='...'` | Ném `HTTPException` 400 `Chỉ có thể đóng ticket từ trạng thái Resolved`. | Đúng với kết quả mong đợi | Pass |
| U_TICK_22 | REQ-68 | UC-23 | reopen_ticket | TicketService | Ticket đang `status='Closed'`, có `id_employee`, có reason hợp lệ. | Kiểm tra mở lại ticket thành công khi ticket đã đóng và đã có người phụ trách. | Valid | `ticket_id`, `reason='Issue reoccurs'` | Ticket chuyển sang `In Progress`; `resolution_note` được reset/null. | Đúng với kết quả mong đợi | Pass |
| U_TICK_23 | REQ-69 | UC-23 | reopen_ticket | TicketService | Ticket `Closed`, `id_employee=None`, reason hợp lệ. | Kiểm tra mở lại ticket sẽ về trạng thái `New` nếu chưa có người phụ trách. | Valid | `ticket_id`, `reason='Need re-open'` | Ticket chuyển sang `New`. | Đúng với kết quả mong đợi | Pass |
| U_TICK_24 | REQ-70 | UC-23 | reopen_ticket | TicketService | Ticket `Closed`, reason rỗng. | Kiểm tra mở lại ticket thất bại khi không cung cấp lý do. | Invalid | `ticket_id`, `reason=''` | Ném `HTTPException` 400 `Vui lòng cung cấp lý do`. | Đúng với kết quả mong đợi | Pass |
| U_TICK_25 | REQ-71 | UC-23 | reopen_ticket | TicketService | Ticket đang `status='Resolved'` chứ chưa `Closed`. | Kiểm tra mở lại ticket thất bại khi trạng thái không phải `Closed`. | Invalid | `ticket_id` của ticket `status='Resolved'`, `reason='...'` | Ném `HTTPException` 400 `Chỉ có thể mở lại ticket từ trạng thái 'Closed'`. | Đúng với kết quả mong đợi | Pass |
| U_CHAT_01 | REQ-72 | UC-24 | send_message | ChatService | `db_session`, `sample_ticket`, `sample_customer`, `mock_notification_service`; customer là participant hợp lệ. | Kiểm tra customer gửi message thành công trong ticket chat. | Valid | `ticket_id`, `sender_id=sample_customer.id`, `content='Hello, I need help with my issue'` | Trả về message object; `message.message` đúng nội dung; `message_type == MessageType.TEXT`. | Đúng với kết quả mong đợi | Pass |
| U_CHAT_02 | REQ-73 | UC-24 | validate_participant | ChatService | `db_session`, `sample_ticket`; tạo outsider customer không gắn với ticket. | Kiểm tra validate participant thất bại khi user không thuộc cuộc trò chuyện. | Invalid | `ticket_id=sample_ticket.id_ticket`, `user_id=outsider.id` | Ném exception/HTTP 403 với nội dung `không có quyền`. | Đúng với kết quả mong đợi | Pass |
| U_CHAT_03 | REQ-74 | UC-24 | validate_participant | ChatService | `db_session`, `sample_customer`; ticket không tồn tại. | Kiểm tra validate participant trả 404 khi ticket không tồn tại. | Invalid | `ticket_id=uuid4()`, `user_id=sample_customer.id` | Ném exception/HTTP 404 với nội dung `Ticket không tồn tại`. | Đúng với kết quả mong đợi | Pass |
| U_CHAT_04 | REQ-75 | UC-25 | get_messages_by_ticket / get_chat_history | MessageRepository / ChatService | `db_session`, `sample_ticket`, `sample_customer`; pre-create 15 messages. | Kiểm tra lấy lịch sử chat có phân trang đúng. | Valid | `ticket_id`, `page=1`, `limit=10` | Trang 1 trả 10 messages; `total == 15`. | Đúng với kết quả mong đợi | Pass |
| U_CHAT_05 | REQ-76 | UC-25 | get_messages_by_ticket / get_chat_history | MessageRepository / ChatService | `db_session`, `sample_ticket_assigned`; không pre-create message nào. | Kiểm tra lấy lịch sử chat khi không có message nào. | Valid | `ticket_id`, `page=1`, `limit=20` | `total == 0`; danh sách message rỗng. | Đúng với kết quả mong đợi | Pass |
| U_CHAT_06 | REQ-77 | UC-26 | get_unread_count | MessageRepository / ChatService | `db_session`, `sample_ticket`, `sample_customer`; pre-create 5 unread messages gửi từ customer. | Kiểm tra đếm số message chưa đọc đúng cho người nhận. | Valid | `ticket_id`, `user_id=sample_ticket.id_employee` | Hàm trả về số unread >= 0 và chỉ tính message từ phía còn lại, chưa đọc, chưa xóa. | Đúng với kết quả mong đợi | Pass |
| U_CHAT_07 | REQ-78 | UC-27 | delete_message | ChatService | `db_session`, `sample_ticket`, `sample_employee`, `sample_message`; message chưa bị xóa. | Kiểm tra xóa mềm message thành công. | Valid | `message_id=sample_message.id_message`, `employee_id=sample_employee.id` | `sample_message.is_deleted == True`; audit log delete được gọi trước khi soft delete. | Đúng với kết quả mong đợi | Pass |
| U_CHAT_08 | REQ-79 | UC-27 | delete_message | ChatService | Có message được tạo sẵn với `is_deleted=True`. | Kiểm tra xóa message thất bại khi message đã bị xóa trước đó. | Invalid | `message_id` của message đã bị xóa, `employee_id` hợp lệ | Ném exception với nội dung `đã bị xóa`. | Đúng với kết quả mong đợi | Pass |
| U_CHAT_09 | REQ-80 | UC-28 | update_message | ChatService | `sample_message` thuộc `sample_ticket`; test truyền `ticket_id=uuid4()` khác ticket thật. | Kiểm tra cập nhật message thất bại khi ticket_id không khớp message. | Invalid | `ticket_id=uuid4()`, `message_id=sample_message.id_message`, `new_content='New content'` | Ném exception với nội dung `không thuộc ticket này`. | Đúng với kết quả mong đợi | Pass |
| U_CHAT_10 | REQ-81 | UC-24 | send_message | ChatService | `db_session`, `sample_ticket`, `sample_customer`, `mock_notification_service`; nội dung message dài 10.000 ký tự. | Kiểm tra gửi message cực dài vẫn được lưu nguyên vẹn vì service không giới hạn độ dài. | Valid | `content='A' * 10000` | Hàm không ném lỗi; `len(message.message) == 10000`. | Đúng với kết quả mong đợi | Pass |
| U_NOTIF_01 | REQ-82 | UC-29 | create_and_send | NotificationService | `db_session`, `sample_customer`; payload notification hợp lệ. | Kiểm tra tạo notification thành công theo happy path cơ bản. | Valid | `NotificationCreate(title='Test Notification', content='Test content', notification_type='TEST', id_receiver=sample_customer.id)` | Notification được tạo với title/type/id_receiver đúng; hàm trả về đối tượng notification vừa tạo. | Đúng với kết quả mong đợi | Pass |
| U_NOTIF_02 | REQ-83 | UC-29 | mark_as_read | NotificationService | Tạo notification `is_read=False` cho đúng owner (`sample_customer`). | Kiểm tra owner đánh dấu notification đã đọc thành công. | Valid | `notification_id`, `user_id=sample_customer.id` | Notification được cập nhật `is_read=True`. | Đúng với kết quả mong đợi | Pass |
| U_NOTIF_03 | REQ-84 | UC-29 | mark_as_read | NotificationService | Notification được tạo cho `sample_customer`, nhưng gọi với `sample_employee.id`. | Kiểm tra đánh dấu đã đọc thất bại khi không đúng người nhận. | Invalid | `notification_id`, `user_id=sample_employee.id` | Ném exception với nội dung `không có quyền`. | Đúng với kết quả mong đợi | Pass |
| U_NOTIF_04 | REQ-85 | UC-30 | get_user_notifications / get_my_notifications | NotificationRepository / NotificationService | Tạo sẵn 5 notifications cho cùng một user. | Kiểm tra phân trang notification bằng skip/limit. | Valid | Page1: `skip=0, limit=2`; Page2: `skip=2, limit=2` | Mỗi page trả về đúng 2 phần tử theo offset/limit. | Đúng với kết quả mong đợi | Pass |
| U_FILE_01 | REQ-86 | UC-31 | sanitize_filename | FileService | Không cần fixture; chỉ test utility thuần với tên file quá dài. | Kiểm tra hàm sanitize_filename cắt bớt tên file quá dài về giới hạn cho phép. | Valid | `filename='a'*150 + '.pdf'` | Tên file sau sanitize có độ dài `<= 105` ký tự; phần tên được cắt về khoảng 100 ký tự + extension. | Đúng với kết quả mong đợi | Pass |
| U_ATTACH_01 | REQ-87 | UC-32 | delete_attachment | AttachmentService | `db_session`, `attachment_service`, `sample_attachment`, `sample_customer`; `delete_file` của Cloudinary được mock. | Kiểm tra uploader xóa mềm attachment thành công. | Valid | `attachment_id=sample_attachment.id_attachment`, `user_id=sample_customer.id` | Hàm trả về `True`; `sample_attachment.is_deleted == True`. | Đúng với kết quả mong đợi | Pass |
| U_GROQ_01 | REQ-88 | UC-33 | chat | GroqService | Mock `httpx.Client`; có 2 API keys; cả 2 lần gọi đều raise `httpx.TimeoutException`. | Kiểm tra GroqService.chat ném lỗi khi tất cả API keys đều timeout. | Invalid | `messages=[{'role':'user','content':'Test'}]` với 2 timeout liên tiếp | Ném exception chứa `All Groq API keys exhausted`. | Đúng với kết quả mong đợi | Pass |
| U_GROQ_02 | REQ-89 | UC-33 | chat | GroqService | Không cần DB; service khởi tạo bình thường. | Kiểm tra GroqService.chat từ chối danh sách message rỗng. | Invalid | `messages=[]` | Ném `ValueError` với nội dung `Messages list cannot be empty`. | Đúng với kết quả mong đợi | Pass |
| U_GROQ_03 | REQ-90 | UC-34 | analyze_sentiment | GroqService | Không cần DB; đầu vào là chuỗi rỗng hoặc chỉ có khoảng trắng. | Kiểm tra analyze_sentiment trả kết quả trung tính khi văn bản đầu vào rỗng. | Valid | `text=''` hoặc `'   '` | Hàm trả về `{label:'neutral', score:0.0}` mà không gọi API Groq. | Đúng với kết quả mong đợi | Pass |
| U_GROQ_04 | REQ-91 | UC-34 | analyze_sentiment | GroqService | Mock httpx trả JSON có `score=1.5` vượt biên. | Kiểm tra score sentiment được chuẩn hóa về miền hợp lệ [-1.0, 1.0]. | Invalid | `text='Test text'`, Groq response `{'label':'positive','score':1.5}` | Hàm trả kết quả với `score == 1.0` sau khi clamp. | Đúng với kết quả mong đợi | Pass |
| U_GROQ_05 | REQ-92 | UC-34 | analyze_sentiment | GroqService | Patch httpx; Groq API trả status 500. | Kiểm tra analyze_sentiment degrade gracefully khi API lỗi. | Invalid | `text='Test text'`, response status=500 | Hàm trả về `label=NEUTRAL`, `score=0.0` thay vì crash. | Đúng với kết quả mong đợi | Pass |
| U_BOT_01 | REQ-93 | UC-35 | _get_public_data | ChatbotService | `db_session`, `sample_department`, `sample_customer_type`; dữ liệu FAQ/department/customer_type tồn tại. | Kiểm tra ChatbotService tổng hợp public data từ nhiều repository thành công. | Valid | Không có input trực tiếp | Result chứa các key `faqs`, `departments`, `customer_types`; aggregation hoạt động đúng. | Đúng với kết quả mong đợi | Pass |
| U_BOT_02 | REQ-94 | UC-35 | check_chatbot_rate_limit | chatbot.py | `db_session`, `sample_customer`, `mock_redis_service`; Redis get trả `'15'` (> limit 10). | Kiểm tra rate limit chặn request khi số lần gọi vượt ngưỡng. | Invalid | Redis count mock = `'15'` cho customer hiện tại | Hàm trả về `False`; request chatbot phải bị block. | Đúng với kết quả mong đợi | Pass |
| U_EVAL_01 | REQ-95 | UC-36 | create_evaluate | EvaluateService | `db_session`, `sample_ticket_resolved`, `sample_customer`, `mock_notification_service`. | Kiểm tra tạo đánh giá thành công cho ticket hợp lệ. | Valid | `EvaluateCreate(id_ticket=resolved_ticket_id, star=5, comment='Excellent service...')` | Evaluate row được tạo; star/comment/id_ticket được lưu đúng. | Đúng với kết quả mong đợi | Pass |
| U_EVAL_02 | REQ-96 | UC-36 | create_evaluate | EvaluateService | `db_session`, `sample_customer`; ticket_id truyền vào không tồn tại. | Kiểm tra tạo đánh giá thất bại khi ticket không tồn tại. | Invalid | `EvaluateCreate(id_ticket=uuid4(), star=5)` | Ném `HTTPException` 404 `Không tìm thấy Ticket!`. | Đúng với kết quả mong đợi | Pass |
| U_EVAL_03 | REQ-97 | UC-36 | _update_employee_csat_score | EvaluateService | `db_session`, `sample_employee`; tạo 3 evaluations có star 5, 4, 3 cho các ticket cùng employee. | Kiểm tra tính điểm CSAT trung bình chính xác từ nhiều đánh giá. | Valid | Nhiều evaluations của cùng employee | `sample_employee.csat_score == 4.0`. | Đúng với kết quả mong đợi | Pass |
| U_EVAL_04 | REQ-98 | UC-36 | _update_employee_csat_score | EvaluateService | `db_session`, `sample_employee`; employee không có evaluation nào. | Kiểm tra điểm CSAT mặc định về 0.0 khi chưa có đánh giá. | Valid | Employee không có rows Evaluate | `sample_employee.csat_score == 0.0`. | Đúng với kết quả mong đợi | Pass |
| U_EVAL_05 | REQ-99 | UC-37 | update_evaluate | EvaluateService | `db_session`, `sample_evaluate` thuộc `sample_customer`; tạo thêm customer khác làm non-owner. | Kiểm tra update đánh giá thất bại khi không phải chủ sở hữu. | Invalid | `evaluate_id=sample_evaluate.id_evaluate`, `EvaluateUpdate(star=1)`, `customer_id=other_customer.id_customer` | Ném `HTTPException` 403 với nội dung không có quyền chỉnh sửa đánh giá này. | Đúng với kết quả mong đợi | Pass |
| U_EVAL_06 | REQ-100 | UC-37 | delete_evaluate | EvaluateService | `db_session`, `sample_evaluate`; customer khác không phải owner. | Kiểm tra xóa đánh giá thất bại khi không phải chủ sở hữu. | Invalid | `evaluate_id=sample_evaluate.id_evaluate`, `customer_id=other_customer.id_customer` | Ném `HTTPException` 403 với nội dung không có quyền xóa đánh giá này. | Đúng với kết quả mong đợi | Pass |
| U_SENT_01 | REQ-101 | UC-38 | get_system_sentiment | SentimentService | `db_session`, `sample_sentiment_report` có year=2026, month=5, scope=system. | Kiểm tra lấy báo cáo sentiment hệ thống thành công khi có dữ liệu kỳ tương ứng. | Valid | `(2026, 5)` | Trả về `SentimentSummaryResponse` đúng year/month/scope; breakdown có `positive=150` theo data mẫu. | Đúng với kết quả mong đợi | Pass |
| U_SENT_02 | REQ-102 | UC-38 | get_system_sentiment | SentimentService | `db_session`; không có sentiment report cho kỳ được hỏi. | Kiểm tra lấy sentiment hệ thống thất bại khi không có dữ liệu kỳ đó. | Invalid | `(2020, 1)` | Ném `HTTPException` 404 `No sentiment data for this period`. | Đúng với kết quả mong đợi | Pass |
| U_SENT_03 | REQ-103 | UC-39 | compare_sentiment | SentimentService | `db_session`; có 2 report cho các kỳ so sánh. | Kiểm tra compare sentiment trả kết quả chênh lệch đúng giữa 2 kỳ. | Valid | `(2026, 4, 2026, 5, 'system')` | Trả về response có `from_period`, `to_period`, `change_absolute` đúng theo dữ liệu assert. | Đúng với kết quả mong đợi | Pass |
| U_SENT_04 | REQ-104 | UC-39 | compare_sentiment | SentimentService | `db_session`; thiếu data ở một hoặc cả hai kỳ. | Kiểm tra compare sentiment thất bại khi thiếu dữ liệu so sánh. | Invalid | `(2020, 1, 2020, 6, 'system')` | Ném `HTTPException` 404 `Missing data for comparison`. | Đúng với kết quả mong đợi | Pass |
| U_SENT_05 | REQ-105 | UC-40 | get_department_sentiment | SentimentService | `db_session`; user thuộc department A, request sentiment của department B. | Kiểm tra chặn truy cập sentiment department khác. | Invalid | `(2026, 5, dept_B_id, requesting_user_dept_id=dept_A_id)` | Ném `HTTPException` 403 `You can only view your own department's sentiment`. | Đúng với kết quả mong đợi | Pass |
| U_SENT_06 | REQ-106 | UC-40 | get_department_trends | SentimentService | `db_session`; user thuộc department A nhưng request trends của department B. | Kiểm tra chặn truy cập trends của department khác. | Invalid | `(2026, dept_B_id, requesting_user_dept_id=dept_A_id)` | Ném `HTTPException` 403. | Đúng với kết quả mong đợi | Pass |
| U_REPORT_01 | REQ-107 | UC-41 | get_system_wide_workload | WorkloadReportService | `db_session`, `sample_ticket_assigned`; employee có ít nhất 1 active ticket và capacity=5. | Kiểm tra tính toán workload hệ thống có employee và ticket đang hoạt động. | Valid | Không có input ngoài DB fixtures | Response có item cho employee; `open_tickets >= 1`; utilization được tính ra. | Đúng với kết quả mong đợi | Pass |
| U_REPORT_02 | REQ-108 | UC-41 | get_department_summary | WorkloadReportService | `db_session`; tạo 1 department inactive trong DB. | Kiểm tra báo cáo department summary loại trừ department inactive. | Valid | Không có input ngoài DB fixtures | Tên `Inactive Department` không xuất hiện trong kết quả. | Đúng với kết quả mong đợi | Pass |
| U_REPORT_03 | REQ-109 | UC-41 | get_department_summary | WorkloadReportService | `db_session`; xóa/không có department active nào trong DB. | Kiểm tra department summary trả empty state đúng khi không có department. | Valid | Không có department active | `result.departments == []`, `result.total_departments == 0`. | Đúng với kết quả mong đợi | Pass |
| U_ESC_01 | REQ-110 | UC-42 | auto_escalate_overdue_tickets | EscalationService | `db_session`, `sample_ticket`; ticket `status='New'`, `expired_date=utcnow()+7days`. | Kiểm tra auto-escalate không xử lý ticket chưa overdue. | Valid | Không có args; DB có ticket chưa overdue | Result: `total_overdue==0`, `escalated==0`, `errors==0`. | Đúng với kết quả mong đợi | Pass |
| U_ESC_02 | REQ-111 | UC-42 | auto_escalate_overdue_tickets | EscalationService | `db_session`, `sample_ticket`, `sample_ticket_category`; ticket `status='Resolved'`, `expired_date` đã quá hạn. | Kiểm tra auto-escalate bỏ qua ticket đã Resolved dù đã quá hạn. | Invalid | Không có args; DB có resolved overdue ticket | `result['total_overdue'] == 0`; ticket resolved bị loại khỏi overdue scan. | Đúng với kết quả mong đợi | Pass |
| U_SET_01 | REQ-112 | UC-43 | update_settings | SystemSettingsService | `db_session`; gọi update 2 lần liên tiếp với 2 field khác nhau. | Kiểm tra partial update bảo toàn các field không bị chỉnh sửa. | Valid | Call1: `company_name='First Company'`; Call2: `support_email='test@example.com'` | Sau lần 2, `company_name` vẫn là `First Company` và `support_email` được cập nhật; chứng minh `exclude_unset=True`. | Đúng với kết quả mong đợi | Pass |
| U_RULE_01 | REQ-113 | UC-44 | create_rule | EscalationRuleService | `db_session`; không cần field optional ngoài bắt buộc. | Kiểm tra tạo escalation rule thành công với chỉ các field tối thiểu. | Valid | `EscalationRuleCreate(name='Minimal Rule', condition_type='category', condition_value='billing', action_type='reassign', action_target=str(uuid4()), is_active=False)` | Rule được tạo thành công; `is_active=False`, `name` đúng, field optional giữ default/null. | Đúng với kết quả mong đợi | Pass |
| U_ADMIN_01 | REQ-114 | UC-45 | reset_password | UserAdminService | `db_session`, `sample_employee`; password mới đủ điều kiện; test kiểm tra round-trip bcrypt. | Kiểm tra reset password của admin service sinh ra hash bcrypt hợp lệ. | Valid | `reset_password('employee', sample_employee.id_employee, 'validPassword456')` | Password hash mới được tạo và `bcrypt.checkpw` xác nhận lại thành công với password gốc. | Đúng với kết quả mong đợi | Pass |

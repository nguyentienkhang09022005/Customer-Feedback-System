| Test Case ID | Related Unit Id | Pre-Condition | Test Case Description | Input Type | Input | Expected Result | Actual Output | Status |
|---|---|---|---|---|---|---|---|---|
| I_AUTH_01 | AuthService, CustomerRepository, OTPService, RedisService, EmailService | Email/username/phone do not exist; DB, Redis, and SMTP are available. | 1. Call customer registration flow with valid payload. 2. Observe CustomerRepository uniqueness check. 3. Observe DB insert of Pending customer. 4. Observe OTP generation, Redis storage, and OTP email send. | Valid | username=newuser01; email=newuser01@example.com; phone=0901111111; password=Secret123 | CustomerRepository confirms identity fields are unique; customer row is created in DB with Pending status; OTPService stores 6-digit OTP in Redis with TTL; EmailService sends OTP message successfully; integration chain completes end-to-end. | Đúng với kết quả mong đợi | Pass |
| I_AUTH_02 | AuthService, CustomerRepository, OTPService, RedisService | Identity fields are unique; DB is available; Redis is unavailable or times out. | 1. Call customer registration flow with valid payload while Redis OTP storage fails. 2. Trace DB write and OTP storage result. | Invalid | Same as valid registration payload, but Redis set_with_expiry fails/timeouts | Customer row is still committed in DB as Pending; OTP is not stored; activation email is not completed; registration flow returns failure/error from OTP stage, exposing orphan Pending-user risk and incomplete integration rollback. | Đúng với kết quả mong đợi | Pass |
| I_AUTH_03 | AuthService, HumanRepository, security.py, ChatbotService, RedisService | Active customer and active employee accounts exist; bcrypt hashes are valid; Redis is available. | 1. Call login for active customer. 2. Call login for active employee/admin. 3. Observe HumanRepository lookup, bcrypt verification, JWT creation, and customer chatbot preload path. | Valid | customer_username/password; employee_username/password | HumanRepository returns matching user; bcrypt verification succeeds; access and refresh JWTs are created with correct claims; for customer login, ChatbotService preloads customer cache to Redis; login response returns tokens successfully. | Đúng với kết quả mong đợi | Pass |
| I_AUTH_04 | AuthService, HumanRepository, security.py | Pending/Banned/invalid user inputs exist; DB is available. | 1. Call login with wrong password. 2. Call login with Pending account. 3. Call login with Banned/Inactive account. 4. Call refresh with malformed/non-UUID sub claim token if applicable. | Invalid | wrong password; pending user; banned user; malformed token | HumanRepository lookup and password/status verification stop the flow before token creation; login returns authentication error; malformed token/user lookup path fails safely and no valid tokens are issued. | Đúng với kết quả mong đợi | Pass |
| I_AUTH_05 | AuthService, TokenBlacklistService, RedisService, ChatbotService | User is logged in with valid refresh token; Redis is available. | 1. Call token refresh. 2. Observe signature/type validation. 3. Observe new token creation. 4. Observe old refresh token blacklisting in Redis. 5. Observe chatbot cache TTL extension for customer. | Valid | refresh_token=valid unblacklisted refresh token | Refresh token is validated; new access and refresh tokens are issued; old refresh token JTI is written to Redis blacklist; customer chatbot cache TTL is extended; old refresh token can no longer be reused. | Đúng với kết quả mong đợi | Pass |
| I_AUTH_06 | AuthService, TokenBlacklistService, RedisService | User is logged in with valid refresh token; Redis blacklist write fails or times out. | 1. Call token refresh while Redis blacklist write is unavailable. 2. Observe whether new token issuance and blacklist update diverge. | Invalid | refresh_token=valid refresh token; Redis failure on blacklist set | New tokens may be issued by AuthService, but old refresh token is not blacklisted because Redis write fails; system returns error on blacklist stage or leaves replay window open, revealing integration risk between token issuance and revocation persistence. | Đúng với kết quả mong đợi | Pass |
| I_AUTH_07 | UserAdminService, EmployeeRepository, CustomerRepository, security.py | Admin account is authenticated; target employee/customer exists. | 1. Call admin update user status. 2. Call admin reset password with valid 6-char+ password. 3. Observe repository fetch, enum validation, bcrypt hash generation, and DB commit. | Valid | user_type=employee/customer; status=Inactive; new_password=Secret6 | Admin-only flow fetches target via correct repository; valid status is saved; password is hashed and committed; subsequent login behavior follows the updated status/password correctly. | Đúng với kết quả mong đợi | Pass |
| I_AUTH_08 | user.py, cloudinary.py, AuthService, HumanRepository | Logged-in user exists; file upload endpoint reachable; Cloudinary and DB are available. | 1. Upload valid image avatar. 2. Observe MIME validation at route layer. 3. Observe Cloudinary upload. 4. Observe avatar URL persistence to DB. | Valid | image/jpeg avatar file | Route accepts image content-type; Cloudinary returns secure URL; AuthService updates user.avatar through HumanRepository; DB reflects the new avatar URL. | Đúng với kết quả mong đợi | Pass |
| I_TICK_01 | TicketService, TicketRepository, SLARepository, TicketHistoryService, NotificationService | Logged-in customer exists; active template exists; related category/department exist; Redis available or gracefully bypassed. | 1. Create ticket from template with valid severity and custom fields. 2. Observe template validation, rate-limit check, SLA lookup, DB insert, history log, and department notification fan-out. | Valid | template_id=active template; title=Printer broken; severity=High; custom_fields=valid map | Template is validated; Redis rate-limit check passes; SLARepository returns policy and expired_date is calculated; ticket is inserted in DB; CREATED history is saved; unassigned-ticket notifications are created for active department members if category/department members exist. | Đúng với kết quả mong đợi | Pass |
| I_TICK_02 | TicketService, RedisService, SLARepository, NotificationService | Active template exists; Redis is unavailable or category members are missing; DB is available. | 1. Create ticket from template while Redis rate-limit service is down. 2. Repeat with severity lacking SLA policy. 3. Repeat with category that has no active members. | Invalid | valid create payload under Redis failure / missing SLA / no members | Ticket creation continues gracefully when Redis rate-limit check fails open; expired_date becomes null if SLA policy is missing; notification fan-out is skipped without crashing when category/department has no members; DB ticket still persists. | Đúng với kết quả mong đợi | Pass |
| I_TICK_03 | TicketService, TicketRepository, HumanRepository, EmailService, TicketHistoryService, Socket.IO broadcaster | Ticket exists and is not deleted; target employee exists; SMTP and Socket.IO are available. | 1. Assign ticket to employee. 2. Observe repository update, employee lookup, email send, Socket.IO assignment event, and history logging. | Valid | ticket_id=existing ticket; employee_id=valid employee | TicketRepository updates id_employee; assignment history is logged; EmailService sends assignment email; real-time assignment event is broadcast; API returns updated ticket successfully. | Đúng với kết quả mong đợi | Pass |
| I_TICK_04 | TicketService, EmailService, Socket.IO broadcaster, LoadBalancer | Ticket exists; employee exists; email and/or Socket.IO may fail; Redis lock may fail if locked assignment path is used. | 1. Assign ticket while email send fails. 2. Assign ticket while Socket.IO broadcast fails. 3. Exercise lock-based assignment when Redis lock cannot be acquired. | Invalid | valid assign payload with simulated email failure / socket failure / Redis lock miss | Core ticket assignment DB update still succeeds even if email or realtime broadcast fails, because those errors are caught as warnings; history is still logged; lock-based assignment path returns no assignment/retry signal when Redis lock is not acquired. | Đúng với kết quả mong đợi | Pass |
| I_TICK_05 | TicketService, TicketHistoryService, SurveyJob, EmailService | Assigned ticket exists in In Progress; survey job scheduler/manual trigger available. | 1. Resolve ticket with resolution note. 2. Observe status transition, resolved_at update, and RESOLVED history entry. 3. After survey delay, run survey job and observe email dispatch plus survey_sent update. | Valid | ticket_id=In Progress ticket; resolution_note=Fixed issue | Resolve flow updates ticket to Resolved and sets resolved_at; history is logged; SurveyJob later selects the ticket, sends CSAT survey email, and marks survey_sent=true, completing the multi-step integration chain. | Đúng với kết quả mong đợi | Pass |
| I_TICK_06 | BulkTicketService, TicketRepository, TagService, TagRepository | Admin is logged in; multiple tickets and tags exist. | 1. Bulk update ticket statuses. 2. Bulk assign tickets. 3. Assign tag to ticket. 4. Remove tag from ticket. 5. Retry duplicate tag assignment and remove-nonexistent-tag paths. | Invalid | bulk status=invalid or mixed states; duplicate tag_id; unassigned tag removal | Valid bulk operations update raw DB rows and return counts; invalid bulk status is rejected; duplicate tag assignment is blocked; removing a non-assigned tag returns error; integration reveals bulk paths bypass transition matrix while tag service enforces junction-table integrity. | Đúng với kết quả mong đợi | Pass |
| I_CHAT_01 | ChatService, MessageRepository, NotificationService, NotificationRepository, Socket.IO | Open ticket exists; sender is participant; receiver exists; DB and Socket.IO notification path are available. | 1. Send ticket chat message. 2. Observe message insert to DB. 3. Observe receiver resolution from ticket. 4. Observe notification persistence and realtime push. | Valid | ticket_id=open ticket; sender_id=customer or assigned employee; message=Need update | MessageRepository creates message row; ChatService finds receiver from ticket linkage; NotificationService persists new notification and emits realtime event to receiver room; message flow succeeds end-to-end. | Đúng với kết quả mong đợi | Pass |
| I_CHAT_02 | ChatService, MessageRepository, AuditLogService, AuditLogRepository | Existing chat message belongs to ticket; employee is authenticated; one message may already be deleted. | 1. Edit employee message content. 2. Delete employee message. 3. Inspect audit log persistence for UPDATE and DELETE payloads. 4. Retry edit on already-deleted message. | Invalid | message_id=active/deleted; new_content=updated text | Valid edit/delete writes message changes and corresponding JSON audit log records with old/new payloads; already-deleted message is rejected before audit write, preventing orphan audit entries for invalid operations. | Đúng với kết quả mong đợi | Pass |
| I_BOT_01 | ChatbotService, RedisService, GroqService | Logged-in customer exists; Redis and Groq are available; customer context exists in DB. | 1. Send chatbot message. 2. Observe Redis cache lookup for customer context. 3. On cache miss, observe DB read and Redis set_with_expiry. 4. Observe prompt assembly and Groq API call. 5. Observe response persistence and session history behavior. | Valid | chatbot_message=What is my ticket status? | ChatbotService reads customer context from Redis or DB; cache is populated/extended appropriately; GroqService returns AI response; chatbot message/response flow completes successfully without cross-customer leakage. | Đúng với kết quả mong đợi | Pass |
| I_BOT_02 | ChatbotService, RedisService, GroqService | Customer is logged in; Redis is down or Groq returns timeout/429/500 across keys. | 1. Send chatbot message while Redis is unavailable. 2. Exceed rate-limit path while increment/check fails. 3. Send chatbot message while Groq times out or all API keys fail. | Invalid | valid chatbot message under Redis/Groq failure conditions | When Redis cache is down, ChatbotService falls back to DB and request may still proceed; rate-limit integration fails open if Redis increment/check errors; when Groq fails across all keys/timeouts, chatbot returns HTTP 503 and no successful AI response is saved. | Đúng với kết quả mong đợi | Pass |
| I_FILE_01 | AttachmentService, FileService, cloudinary.py, AttachmentRepository | Logged-in user exists; valid file is prepared; Cloudinary and DB are available. | 1. Upload valid allowed file. 2. Observe extension/size/content validation. 3. Observe Cloudinary upload and thumbnail generation. 4. Observe attachment metadata persistence. | Valid | valid .png/.pdf/.docx under 10MB | FileService validates file successfully; Cloudinary stores original asset and generates URL/thumbnail; attachment row is inserted in DB with storage metadata; upload flow completes without partial loss. | Đúng với kết quả mong đợi | Pass |
| I_FILE_02 | AttachmentService, FileService, cloudinary.py | Logged-in user exists; invalid files and Cloudinary failure path are prepared. | 1. Upload empty/oversized/disallowed/MIME-mismatched file. 2. Upload valid file while Cloudinary upload fails. 3. Delete stored attachment while Cloudinary delete fails. | Invalid | empty file; 11MB file; file.exe; spoofed MIME; Cloudinary outage | Invalid files are rejected before external upload; if Cloudinary upload fails, no DB attachment row is committed; if Cloudinary delete fails during soft delete, DB soft delete still proceeds and may leave orphan external asset, which must be handled by cleanup flow. | Đúng với kết quả mong đợi | Pass |
| I_EVAL_01 | EvaluateService, EvaluateRepository, NotificationService, EmployeeRepository | Customer owns or can access ticket; assigned employee exists; DB and notification path are available. | 1. Submit evaluation with star/comment for ticket. 2. Observe ticket existence check, evaluation insert, employee notification creation, and csat_score recomputation. 3. Repeat with multiple evaluations to verify averaging. | Valid | id_ticket=existing ticket; star=3 then 5; comment=Good support | Evaluate row is inserted; if ticket has assigned employee, notification is created and sent; employee csat_score is recalculated as average of all related evaluations; DB values are consistent across all linked units. | Đúng với kết quả mong đợi | Pass |
| I_EVAL_02 | EvaluateService, NotificationService, EmployeeRepository | Non-existent ticket, ticket without assigned employee, or simultaneous submissions exist for testing. | 1. Submit evaluation for non-existent ticket. 2. Submit evaluation for ticket without assigned employee. 3. Submit two evaluations concurrently for same ticket. | Invalid | id_ticket=random UUID / ticket with no employee / duplicate concurrent posts | Non-existent ticket is rejected before any DB insert or notification; ticket without assigned employee still stores evaluation but skips notification; concurrent evaluations create multiple rows and all are included in csat_score aggregation without crashing. | Đúng với kết quả mong đợi | Pass |
| I_SENT_01 | SentimentAnalysisJob, GroqService, SentimentDetail, SentimentReport | Messages/evaluations/comments exist in last 7 days; job runner and Groq API keys are configured. | 1. Run sentiment analysis job. 2. Observe per-item call to GroqService. 3. Observe SentimentDetail inserts. 4. Observe aggregate SentimentReport updates for system and departments. | Valid | mixed message/evaluation/comment contents including long text | GroqService analyzes/truncates input as needed; SentimentDetail rows are saved; aggregate SentimentReport counters and average scores are updated for system and department scopes; pipeline completes batch by batch. | Đúng với kết quả mong đợi | Pass |
| I_SENT_02 | SentimentAnalysisJob, GroqService | Sentiment job data exists; Groq returns timeouts, 500s, malformed JSON, or all keys exhausted. | 1. Run sentiment analysis job under Groq timeout/failure conditions. 2. Observe key rotation, neutral fallback on malformed/empty output, and partial-commit behavior. | Invalid | Groq timeout; 500 response; malformed JSON; empty content | Job rotates keys on upstream failures; malformed/empty content falls back to neutral result safely; errors are counted without crashing the entire job; partial successful rows remain committed and report generation proceeds with available data. | Đúng với kết quả mong đợi | Pass |
| I_REPORT_01 | WorkloadReportService, EmployeeRepository, TicketRepository, DepartmentRepository | Admin is authenticated; employees, capacities, departments, and tickets in mixed statuses exist. | 1. Request system-wide workload report. 2. Request department summary report. 3. Observe employee repo fetch, active ticket counts, utilization math, department lookup, and aggregate calculations. | Valid | empA capacity=5 open=2; empB capacity=0 open=5; inactive dept present | Service aggregates data from multiple repositories correctly; only active ticket statuses are counted; utilization handles 0 capacity safely; over-capacity can exceed 100%; inactive departments are excluded from department summary. | Đúng với kết quả mong đợi | Pass |
| I_JOB_01 | SLABreachJob, EscalationService, NotificationService, EmployeeRepository, EmailService | Overdue ticket, near-expiry ticket, resolved ticket, manager routing, and survey-eligible resolved ticket exist; scheduler/manual runner available. | 1. Run SLA breach job and observe warning/breach/escalation chain. 2. Run survey job and observe pending-ticket selection, email send, and survey_sent commit. 3. Repeat under notification or SMTP failure. | Invalid | overdue active ticket; near-expiry ticket; resolved ticket; simulated NotificationService/SMTP failure | SLA job notifies assigned employee and manager for overdue active tickets, warns within 24h window, and skips resolved/closed/no-employee cases gracefully; survey job sends email and marks survey_sent=true on success; notification or SMTP failures are caught/logged so batch processing continues, and failed survey emails are retried on later runs because survey_sent is not set. | Đúng với kết quả mong đợi | Pass |
| I_DEPT_01 | DepartmentAssignmentService, EmployeeRepository, DepartmentRepository | Admin is authenticated; departments and employees exist, including manager-role constraints. | 1. Assign employee to department. 2. Transfer employee between departments. 3. Set/remove manager under valid and invalid manager-constraint scenarios. 4. Observe pre-write validation chain and final commit. | Invalid | employee_id=random UUID; department_id=random UUID; assign second manager; remove last manager | Service validates employee existence, department existence, and single-manager rules before any write; invalid chains return 404/400 without DB mutation; valid assignment/transfer/set-manager operations commit successfully through repository layer. | Đúng với kết quả mong đợi | Pass |

| Test Case ID | Group | UI | UC Id | Pre-Condition | Test Case Description | Input Type | Input | Expected Result | Actual Output (Cycle 1) | Status (Cycle 1) | Actual Output (Cycle 2) | Status (Cycle 2) | Actual Output (Cycle 3) | Status (Cycle 3) |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| S_AUTH_01 | Authentication | Register | UC-01 | User is not logged in; email/username/phone do not exist; OTP service is available. | 1. Open Register screen. 2. Enter valid username, email, password, first name, last name, and phone. 3. Submit registration form. 4. Retrieve OTP sent to the registered email. 5. Enter OTP on Verify OTP screen. 6. Login with the new account. | Valid | username=newcustomer01; email=newcustomer01@example.com; password=Secret123; phone=0901234567; otp=valid 6-digit code within 5 minutes | Registration succeeds and creates customer in Pending state; OTP is accepted once; account becomes Active after verification; login returns access and refresh tokens; user can access own profile successfully. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_AUTH_02 | Authentication | Verify OTP | UC-02 | Customer account exists in Pending state after registration. | 1. Open Verify OTP screen. 2. Enter wrong 6-digit OTP. 3. Retry with expired OTP after 5 minutes. 4. Retry with a reused OTP that was already accepted once. | Invalid | otp=wrong 6-digit; otp=expired code; otp=reused code | System rejects all invalid OTP cases; account remains Pending; clear validation/error message is shown; no tokens are issued. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_AUTH_03 | Authentication | Login | UC-03 | Active Customer, Employee, Admin, and Manager accounts exist; separate Pending/Banned accounts also exist. | 1. Login with valid Customer credentials. 2. Login with valid Employee/Admin/Manager credentials. 3. Login with wrong password. 4. Login with Pending account. 5. Login with Banned/Inactive account. | Invalid | valid active users; wrong_password; pending_user; banned_user | Active users receive valid JWT tokens with correct role/type claims; wrong password and non-active accounts are rejected with authentication error; unauthorized users cannot proceed. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_AUTH_04 | Authentication | Forgot Password / Reset Password | UC-04 | User account exists and can receive OTP email. | 1. Open Forgot Password. 2. Submit existing email. 3. Retrieve reset OTP. 4. Submit Reset Password with matching new password and confirm password. 5. Login with old password. 6. Login with new password. 7. Repeat forgot-password with non-existent email. | Valid | email=existing user; otp=valid reset OTP; new_password=Newpass123; confirm_password=Newpass123; non_existent_email=test404@example.com | Existing user can reset password successfully; old password no longer works; new password works; request with unknown email still returns generic success message and does not reveal account existence. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_AUTH_05 | Authentication | Token Refresh / Logout | UC-05 | User is logged in and has valid access and refresh tokens. | 1. Call Refresh Token using a valid refresh token. 2. Retry refresh with the old refresh token. 3. Logout with access token only. 4. Try to call protected endpoint using the blacklisted access token. 5. Logout with both access and refresh tokens. 6. Retry refresh with blacklisted refresh token. | Invalid | refresh_token=used old token; access_token=logged out token | Valid refresh returns a new token pair; reused old refresh token is rejected; logged-out access token is rejected on protected APIs; full logout revokes both tokens. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_AUTH_06 | Authentication | My Profile / Change Password / Avatar | UC-06 | Logged-in user exists with valid access token. | 1. Open My Profile. 2. Update first name only. 3. Change password with correct old password. 4. Retry change password with wrong old password. 5. Upload valid image avatar. 6. Retry avatar upload with non-image file. | Invalid | old_password=wrong one; avatar=file.pdf | Partial profile update preserves unspecified fields; correct password change succeeds; wrong old password is rejected; valid image avatar uploads successfully; non-image file is rejected. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_RBAC_01 | Authorization | Admin User Management | UC-07 | Admin, Manager, Employee, and Customer accounts exist and can log in. | 1. Login as Admin and call employee list, customer list, update user status, and reset password admin screens. 2. Login as Manager and try admin-only user status update. 3. Login as Employee and try admin-only pages. 4. Login as Customer and try employee management pages. | Invalid | actor_role=Manager/Employee/Customer on admin pages | Admin can access admin-only functions; Manager/Employee/Customer are blocked from Admin-only endpoints with authorization error; Customer can still access own profile only. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_TICK_01 | Ticket Management | Create Ticket from Template | UC-08 | Logged-in customer exists; active template exists; template category and SLA policy exist. | 1. Open Create Ticket From Template. 2. Select active template. 3. Enter valid title, severity, and custom fields. 4. Submit. 5. Open created ticket detail. | Valid | template_id=active template; title=Internet outage in branch office; severity=High; custom_fields=valid required values | Ticket is created successfully with status New; SLA expired date is calculated from severity/policy; history contains CREATED event; category/department linkage is preserved. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_TICK_02 | Ticket Management | Create Ticket from Template | UC-09 | Logged-in customer exists; one inactive template, one soft-deleted template, and one non-existent template ID are available for testing. | 1. Try to create ticket from inactive template. 2. Try to create ticket from soft-deleted template. 3. Try to create ticket from random template UUID. | Invalid | template_id=inactive template / deleted template / random UUID | Inactive template is rejected; deleted template is rejected; non-existent template returns not-found error; no ticket is created. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_TICK_03 | Ticket Management | Create Ticket from Template | UC-10 | Logged-in customer exists and can create tickets repeatedly within the same hour window. | 1. Create 5 valid tickets within the configured rate-limit window. 2. Attempt to create the 6th ticket in the same window. | Invalid | 6 sequential valid create requests within 1 hour | First 5 ticket creations succeed; the 6th request is blocked with rate-limit error; database contains only the 5 successful tickets. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_TICK_04 | Ticket Management | Assign Ticket | UC-11 | New ticket exists; Admin or eligible Manager is logged in; target employee exists. | 1. Open ticket detail. 2. Assign ticket to employee. 3. Verify assigned employee on ticket. 4. Check notifications/history. | Valid | ticket_id=existing New ticket; employee_id=valid employee in correct scope | Ticket is assigned successfully; assigned employee is saved; assignment history is logged; employee receives assignment notification. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_TICK_05 | Ticket Management | Status Workflow | UC-12 | Assigned ticket exists in New status; employee is logged in. | 1. Change status New → In Progress. 2. Change In Progress → Pending. 3. Change Pending → Resolved with resolution note. 4. Customer closes Resolved ticket with close reason. | Valid | status flow=New→In Progress→Pending→Resolved→Closed; resolution_note and close reason provided | Each valid transition succeeds; resolved_at is stored on Resolve; close action succeeds only after Resolved; ticket history records every transition and actor. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_TICK_06 | Ticket Management | Status Workflow | UC-13 | Tickets exist in New, In Progress, Closed, and Cancelled states; employee is logged in. | 1. Try to resolve a New ticket directly. 2. Try to close a New ticket. 3. Try to close an In Progress ticket. 4. Try to transition Closed ticket again. 5. Try invalid status string. 6. Try employee update with title/custom field changes instead of status-only change. | Invalid | invalid transitions; status=X_UNKNOWN; employee payload modifies title/custom_fields | System rejects forbidden transitions and invalid status values; employee cannot modify restricted fields; no invalid transition is saved in DB. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_TICK_07 | Ticket Management | Customer Ticket Edit | UC-14 | Customer owns a ticket in New status; another owned ticket exists in non-New status. | 1. Customer edits custom fields of owned New ticket. 2. Customer retries editing after ticket moves to In Progress. | Invalid | owned New ticket update; owned In Progress ticket update | Update succeeds only while ticket is New; once ticket is not New, customer update is rejected; ownership is still enforced. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_TICK_08 | Ticket Management | Reopen Ticket | UC-15 | Closed ticket exists; customer owns the ticket; one version has assigned employee and one version has no assignee. | 1. Customer reopens Closed ticket with valid reason when ticket has assignee. 2. Customer reopens another Closed ticket with valid reason when ticket has no assignee. 3. Retry reopen with empty reason. 4. Retry reopen from non-Closed ticket. | Invalid | reopen_reason=empty/whitespace; ticket_status=Resolved | Reopen with assignee changes status to In Progress; reopen without assignee changes status to New; empty reason is rejected; non-Closed ticket cannot be reopened; reopen history is logged and employee notification is sent when applicable. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_TICK_09 | Ticket Management | Customer / Manager Access Control | UC-16 | Ticket belongs to Customer A; Customer B exists; Manager of Dept X and ticket in Dept Y exist. | 1. Customer B opens Customer A ticket detail. 2. Customer B tries to close Customer A ticket. 3. Manager of Dept X views Dept Y tickets. 4. Manager of Dept X assigns ticket in Dept Y. 5. Eligible Manager of correct department assigns ticket in own department. | Invalid | actor=Customer B or wrong-department Manager | Non-owner customer cannot view or operate on another customer's ticket; wrong-department manager cannot view/assign another department's tickets; correct manager can assign within own department only. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_TICK_10 | Ticket Management | Bulk Ticket Operations | UC-17 | Multiple tickets exist in mixed states; Admin is logged in. | 1. Bulk update status for multiple tickets. 2. Bulk assign tickets to an employee. 3. Bulk delete tickets. 4. Retry bulk update with empty list. 5. Retry with invalid status. | Invalid | status=invalid; ticket_ids=[] | Valid bulk operations succeed and return counts; empty-list operation returns zero updated/deleted cleanly; invalid status is rejected. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_CHAT_01 | Interaction | Real-time Ticket Chat | UC-18 | Customer and assigned employee are logged in; ticket is Open (not Closed); Socket.IO connection is available. | 1. Customer joins ticket chat room. 2. Send message to assigned employee. 3. Employee receives real-time event. 4. Employee replies. 5. Customer receives real-time event and notification. | Valid | ticket_id=open ticket; message=valid text content | Messages are saved successfully; chat history is preserved; real-time broadcast reaches ticket room; receiver notification is created and sent. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_CHAT_02 | Interaction | Real-time Ticket Chat | UC-19 | Closed ticket exists; customer or employee is connected to Socket.IO. | 1. Join chat room for Closed ticket. 2. Attempt to send message to Closed ticket. | Invalid | message=any content on Closed ticket | System rejects sending message to Closed ticket; sender receives error event; no DB message record is created. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_CHAT_03 | Interaction | Chat History / Unread Count | UC-20 | Ticket has existing unread messages for the current participant. | 1. Open chat history with page/limit within allowed range. 2. Check unread count. 3. Mark messages as read. 4. Refresh unread count. 5. Retry history/unread/read actions as unrelated non-participant. | Invalid | limit=0; limit=101; actor=non-participant | Valid page/limit returns history correctly; unread count matches unread messages; mark-as-read sets unread count to 0; non-participant gets authorization error; invalid limit gets request validation error. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_CHAT_04 | Interaction | Message Edit / Delete | UC-21 | Employee-authenticated chat message exists; one message is already soft-deleted. | 1. Employee edits own ticket message content. 2. Employee deletes message. 3. Retry editing deleted message. 4. Retry deleting deleted message. 5. Retry edit using mismatched ticket ID. 6. Customer tries to call employee-only edit/delete API. | Invalid | deleted message; wrong ticket_id; customer actor | Valid edit/delete succeeds and audit log is recorded; deleted message cannot be edited or deleted again; mismatched ticket/message pairing is rejected; customer is blocked from employee-only APIs. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_BOT_01 | Chatbot | Chatbot Message | UC-22 | Logged-in customer exists; chatbot service and Redis are available. | 1. Open chatbot. 2. Send up to 10 valid messages within 1 minute. 3. Retrieve chatbot session history. 4. Delete chatbot session. | Valid | 10 messages/minute; valid customer JWT | First 10 chatbot messages are accepted; AI responses are stored; session history can be retrieved; delete session removes stored session/messages. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_BOT_02 | Chatbot | Chatbot Message | UC-23 | Logged-in customer exists and can send repeated chatbot requests in one minute. | 1. Send 10 chatbot messages within the same minute. 2. Send the 11th message before rate-limit window expires. | Invalid | 11th chatbot request within 60 seconds | First 10 messages succeed; the 11th request is blocked with HTTP 429 rate-limit error; no extra message is processed by AI for the blocked request. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_ATTACH_01 | Attachments | File Upload | UC-24 | Logged-in user exists; upload endpoint is reachable. | 1. Upload valid attachment within 10 MB and allowed MIME/extension. 2. Open attachment list for the related reference. 3. Delete own uploaded attachment. | Valid | file=valid .png/.pdf/.docx/.xlsx under 10MB | Allowed file uploads successfully; attachment metadata is stored; file appears in reference list; uploader can soft-delete own attachment successfully. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_ATTACH_02 | Attachments | File Upload | UC-25 | Logged-in user exists; disallowed and oversized files are prepared. | 1. Upload empty file. 2. Upload oversized file >10 MB. 3. Upload disallowed extension such as .exe. 4. Upload MIME/content mismatch file. 5. Try deleting attachment uploaded by another user. | Invalid | empty file; 11MB file; file.exe; spoofed MIME; other user attachment delete | System rejects empty, oversized, disallowed, and MIME-mismatched files; unauthorized delete is rejected; no invalid file is accepted into the system. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_EVAL_01 | Evaluation | Submit CSAT Evaluation | UC-26 | Ticket exists; customer owns the ticket; survey email may already have been sent. | 1. Customer opens evaluation form. 2. Submit star=5 with comment. 3. Open employee profile/report or workload metrics. | Valid | star=5; comment=Excellent support | Evaluation is created successfully; employee receives evaluation notification; employee CSAT score is recalculated based on saved evaluations. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_EVAL_02 | Evaluation | Submit / Update / Delete Evaluation | UC-27 | Evaluation exists; customer owner and non-owner customer both exist. | 1. Submit evaluation with star=0. 2. Submit evaluation with star=6. 3. Customer B tries to update/delete Customer A evaluation. 4. Customer A updates/deletes own evaluation. | Invalid | star=0; star=6; actor=non-owner customer | Star outside 1-5 is rejected by validation; non-owner customer cannot update/delete another customer's evaluation; owner can update/delete own evaluation successfully. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_SENT_01 | Analytics | Sentiment Analysis Job | UC-28 | Messages, evaluations, and comments exist in the last 7 days; scheduled job or manual trigger is available. | 1. Seed data from all 3 sources: message, evaluation, comment. 2. Run sentiment analysis job. 3. Open sentiment reports and details. 4. Repeat with empty comment and very long text content. | Valid | message/evaluation/comment sources; long text >1000 chars; null comment | Job processes all three sources; detail rows and aggregated reports are generated; long text is truncated safely; null/empty content falls back safely without crashing the job. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_ANALYT_01 | Analytics | Sentiment Dashboard | UC-29 | Sentiment reports exist for selected months and departments; Admin account exists. | 1. Login as Admin. 2. Open summary by month/year. 3. Open trends for a month range. 4. Compare two periods. 5. Open by-department analytics. 6. Retry request with missing data period and invalid month/year. 7. Retry as non-admin. | Invalid | month=0 or 13; year=2019; period without data; actor=non-admin | Admin can access analytics summary, trends, compare, and department reports; invalid month/year is rejected; missing-data period returns not-found; non-admin is blocked from analytics endpoints. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_REPORT_01 | Reports | Workload Dashboard | UC-30 | Admin exists; employees with different capacities, ticket counts, departments, and CSAT scores exist. | 1. Open system-wide workload report. 2. Open department workload summary. 3. Verify employee utilization for 0 capacity, equal capacity, and over-capacity cases. 4. Verify inactive department handling. | Valid | employeeA capacity=5 open=2; employeeB capacity=1 open=1; employeeC capacity=5 open=6; employeeD capacity=0 open=5 | Workload report calculates utilization accurately, including 40%, 100%, >100%, and 0% when capacity is zero; only active statuses count toward workload; inactive departments are excluded from department summary; CSAT values are displayed consistently. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_ADMIN_01 | Admin Operations | Escalation Rules | UC-31 | Admin is logged in; escalation rule screen is available. | 1. Create escalation rule. 2. View rule list. 3. Toggle active→inactive and back. 4. Update rule. 5. Delete rule. 6. Retry GET/PUT/DELETE with random UUID. | Invalid | random rule UUID | Valid CRUD and toggle operations succeed for Admin; random UUID returns not-found; toggle changes active state each time correctly. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_ADMIN_02 | Admin Operations | System Settings | UC-32 | Admin is logged in; system settings row may or may not exist yet. | 1. Open system settings. 2. Enable maintenance_mode only. 3. Save. 4. Re-open settings and verify other fields remain unchanged. 5. Disable registration-related flag. 6. Retry update with invalid field payload. | Invalid | invalid field in settings payload | Settings screen supports partial update; unspecified fields remain unchanged; default settings row is auto-created when missing; invalid payload is rejected. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_ADMIN_03 | Admin Operations | User Status / Password Reset | UC-33 | Admin is logged in; target Customer and Employee accounts exist. | 1. Change user status from Active to Inactive. 2. Try login with that user. 3. Change status to Banned. 4. Reset password with exactly 6 characters. 5. Retry reset with 5-character password. 6. Retry status update with invalid status string. | Invalid | new_password=12345; status=UnknownStatus | Inactive/Banned user cannot log in; 6-character reset password is accepted; 5-character password is rejected; invalid status string is rejected with clear error. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_DEPT_01 | Admin Operations | Department Management | UC-34 | Admin/employee with access to department management exists. | 1. Create department with unique name. 2. Update department name. 3. Retry create with duplicate name. 4. Retry update to another existing department name. 5. Create department using Unicode/special characters in name. | Invalid | duplicate department name | Unique department creation/update succeeds; duplicate names are rejected; Unicode/special-character name within length limit is accepted. | Đúng với kết quả mong đợi | Pass | - | - | - | - |
| S_JOB_01 | Operational Automation | SLA Breach / Survey Jobs | UC-35 | Overdue active ticket, near-expiry ticket, and resolved ticket older than survey delay exist; assigned employee and department manager exist where applicable. | 1. Run SLA breach check job. 2. Verify warning for ticket within 24h of expiry. 3. Verify breach notification for overdue active ticket. 4. Verify manager escalation when department manager exists. 5. Run survey job for resolved tickets after delay. 6. Re-run survey job to verify idempotency. | Valid | ticket A expiry in 3h; ticket B expired 1 day ago; ticket C resolved >1h ago and survey_sent=false | SLA warning is sent only within warning window; overdue active ticket triggers employee and manager notifications; resolved/closed tickets are skipped by SLA check; survey email is sent after delay and survey_sent becomes true; rerun does not resend already-sent survey. | Đúng với kết quả mong đợi | Pass | - | - | - | - |

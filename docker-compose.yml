version: '3.8'

services:
  app:
    build: .
    container_name: customer-feedback-system
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    env_file:
      - .env
    dns:
      - 8.8.8.8
      - 1.1.1.1
    command: uvicorn main:app --host 0.0.0.0 --port 8080 --reload